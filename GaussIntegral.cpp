#include <stdio.h>
#include <stdlib.h>

// Gauss积分计算器
// 输入函数指针的原型默认为double(double)
// 传入的函数区间默认为二维数组

double GaussIntegral(double* funIntegral,double* basis1, double* basis2, double[] integralInterval) {
    double gaussIntegralPoints = new double[];
}
